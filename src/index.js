import './sass/main.scss'

document.addEventListener('DOMContentLoaded', () => {
  const slider = require("./js/slider")
  const navbar = require('./js/navbar')
  const parallax = require('./js/parallax')
  const form = require('./js/form')
  const animation = require('./js/animation')
  const scrollTo = require('./js/scrollTo')
  const SmoothScroll = require('./js/smoothScroll')
})